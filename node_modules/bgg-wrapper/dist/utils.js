"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.findFirst = exports.timeout = void 0;
const t = (n, s) => {
    switch (s) {
        case "milliseconds":
            return n;
        case "seconds":
            return n * 1000;
        case "minutes":
            return n * 1000 * 60;
        case "hours":
            return n * 1000 * 60 * 60;
    }
};
function timeout(n, s) {
    if (s) {
        return new Promise((res, _) => {
            setTimeout(res, t(n, s));
        });
    }
    else {
        return new Promise((res, _) => {
            setTimeout(res, n);
        });
    }
}
exports.timeout = timeout;
const findFirst = (arr, search, get) => {
    for (let s of search) {
        for (let el of arr) {
            if (get(el) === s) {
                return s;
            }
        }
    }
    return null;
};
exports.findFirst = findFirst;
